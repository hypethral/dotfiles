mod ex1::excerise_one;

pub fn main() {
    // thread::spawn(|| {
    //     loop {
    //         println!("this is a spawn!!");
    //         thread::sleep(Duration::from_millis(1));  
    //     }
    // });
    // println!("Hello from the main thread!");
}